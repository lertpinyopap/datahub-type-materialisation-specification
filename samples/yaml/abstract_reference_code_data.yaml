# yaml-language-server: $schema=../../schema/type-materialisation.schema.json
id: abstract_reference_code_data
description: Shared shape for reference code data tables.
control_data:
  materialisation_type: table
  failure_mode: quarantine_row
  quarantine:
    schema: TMP
    table: reference_code_QUARANTINE
source:
  format: table
  schema: TMP
  table: abstract_reference_code_data
target:
  id: abstract_reference_code_data
  schema: TMP
  fields:
    - id: code
      source:
        column: code
      data_type: varchar(50)
      nullable: false
      unique: true
