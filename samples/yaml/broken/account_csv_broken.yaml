# yaml-language-server: $schema=../../../schema/type-materialisation.schema.json
id: 1_broken_account_csv
description: Intentionally broken sample for parser diagnostics.
control_data:
  materialisation_type: table
  failure_mode: quarantine_row
source:
  format: csv
  header: true
target:
  id: account
  schema: TMP
  fields:
    - id: account_id
      source:
        pos: 0
        column: account_id
      data_type: made_up_type
      nullable: false
      validations:
        - type: regex
          pattern: "["
    - id: ACCOUNT_ID
      source:
        pos: 1
        column: account_name
      data_type: varchar(255)
      nullable: true
