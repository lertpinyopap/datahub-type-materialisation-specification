# Full local development install. Runtime packaging uses requirements-runtime.txt
# so Airflow can provide dbt-core/dbt-snowflake separately.
-r requirements-runtime.txt

# Stay on the dbt 1.12 line until we intentionally plan an upgrade.
dbt-snowflake>=1.11.6,<1.13
dbt-core>=1.12.0,<1.13
snowflake-connector-python[secure-local-storage]==4.7.1

setuptools>=77
wheel>=0.45
pytest>=8.0
build>=1.2
